#!/bin/bash
IFS=$'\n'
urls="$(grep -hi '^url=' "$@" | cut -d'=' -f2-)"
profile_selected=false
for url in $urls; do
	if [ "$profile_selected" = false ]; then
		firefox --ProfileManager -- "$url"
		profile_selected=true
	else
		firefox -- "$url" &
	fi
done
